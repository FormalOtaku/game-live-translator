# MIGRATION_PLAN

## Current Baseline
- Runtime schema version: `1` at the executable SQL/repository boundary.
- First schema seed: `app_meta.schema_version = 1`.
- v1 persistence target: SQLite in the user's app data directory.
- Current implementation note: T-007-002 defines schema SQL and repository adapter calls without binding to a concrete Node SQLite driver. The production Python FastAPI sidecar must preserve the same schema/parameter contract when it owns the real SQLite connection.

## Trigger
- What change requires migration: any change after schema implementation that alters profile, settings, glossary, translation cache, overlay theme, app metadata, profile export, or API contract compatibility.
- Owner: codex until project ownership is reassigned.

## Compatibility Strategy
- Backward compatibility window: after v1.0, support the current profile export schema and at least one previous compatible export schema.
- Dual-read / dual-write strategy: not needed before v1 schema exists. If a future storage refactor happens, implement dual-read first, then migrate, then remove old reads in a later major release.
- Secret handling: API keys are never migrated through SQLite or JSON. Key migrations must use OS secure storage APIs directly and must not expose values in logs or diagnostic bundles.

## Initial Schema Plan
1. `profiles`: profile identity, display name, optional game title, timestamps.
2. `profile_settings`: capture source, ROI JSON, OCR preset, provider id, target language, theme id, capture frequency, confidence floor.
3. `glossary_terms`: per-profile source term, target term, note.
4. `translation_cache`: provider/source hash/glossary revision metadata entries; no raw source text or translated text payloads in the default schema.
5. `overlay_themes`: theme display name and CSS/token JSON.
6. `privacy_settings`: singleton privacy settings row seeded from `DEFAULT_PRIVACY_SETTINGS`.
7. `app_meta`: schema version and feature flags.

## Schema Version 1 Boundary
- `src/storage/sqlite-config-store.js` is the executable schema/repository contract for T-007. It accepts an injected SQLite-like adapter and does not add a Node SQLite dependency.
- Fresh databases run all schema statements, set `app_meta.schema_version` to `"1"`, insert built-in overlay themes, and insert the default privacy settings row without overwriting later user changes.
- Initialization must not overwrite a non-`1` `app_meta.schema_version`; future migrations own forward movement from version 1.
- `privacy_settings` is a singleton row with `id = 1`; profile/configuration tables persist ISO timestamp columns so update ordering and migration audits remain observable.
- Profile writes validate `ProfileCreateRequest` and compute glossary revision before any SQL write, then persist `profiles`, `profile_settings`, and `glossary_terms`.
- Privacy settings writes validate `PrivacySettings` before any SQL write and store booleans as integer `0`/`1`.
- Provider API keys are excluded from all SQLite schema and repository methods. Key migrations must remain an OS secure storage concern.

## No-Migration Runtime Slices
- 2026-05-28 T-005-005: OCR-to-overlay runtime pipeline is in-memory only. It introduces no SQLite tables, no profile export schema changes, no cache persistence, and no key storage movement. Future persistence slices must decide explicitly which sanitized pipeline fields, if any, are durable.
- 2026-05-28 T-006-001: Localhost HTTP server core is in-memory only. It introduces no SQLite tables, no profile export schema changes, no persistent status store, and no key storage movement. Port selection and runtime status are process state only.
- 2026-05-28 T-006-002: OBS overlay HTML renderer is stateless and self-contained. It introduces no SQLite tables, no theme persistence, no profile export schema changes, and no key storage movement. Built-in theme CSS in this slice is runtime code only.
- 2026-05-28 T-006-003: Overlay WebSocket replay and broadcast state is in-memory only. It introduces no SQLite tables, no durable client sessions, no profile export schema changes, no cache persistence, and no key storage movement.
- 2026-05-28 T-006-004: `/ws/app` app status stream and runtime error mapping are in-memory only. They introduce no SQLite tables, no durable status store, no profile export schema changes, no cache persistence, and no key storage movement. Connected clients receive sanitized `AppStatus` snapshots derived from `OverlayState` and process-level runtime status; no migration is needed.
- 2026-05-28 T-006-005: `npm run smoke:server` and the server smoke runbook add no runtime persistence. The smoke uses in-memory fixtures and an ephemeral localhost port only; no SQLite migration, key storage migration, profile export migration, or cache migration is needed.
- 2026-05-28 T-007-001: Profile/settings contract validation is a pure validator slice in `src/contracts/`. It introduces no SQLite tables, no key storage movement, no cache persistence, and no profile export schema bump (`PROFILE_EXPORT_SCHEMA_VERSION` stays at `1`). `DEFAULT_PRIVACY_SETTINGS` is documented as the privacy-first seed that the future first-run/T-007 persistence slice must write on a fresh database; no migration is needed because there is no prior persisted PrivacySettings shape.
- 2026-05-28 T-007-003: Profile CRUD, active profile selection, and `ProfileExport` use the existing schema version 1 tables and `app_meta.active_profile_id`. This introduces no schema bump, no provider key storage movement, no cache persistence change, and no profile export schema bump (`PROFILE_EXPORT_SCHEMA_VERSION` remains `1`). Future migrations that alter profile rows must preserve active-profile load and safe export validation.
- 2026-05-28 T-007-004: Theme CRUD and glossary import/export use the existing schema version 1 `overlay_themes`, `glossary_terms`, and `profile_settings.glossary_revision` columns. This introduces no schema bump, no key storage movement, no cache persistence change, and no profile export schema bump. Future migrations that alter theme CSS JSON or glossary terms must preserve built-in read-only guards, theme-in-use checks, and all-or-nothing glossary import behavior.
- 2026-05-28 T-007-005: Privacy settings read/update uses the existing schema version 1 `privacy_settings` singleton row. Provider key write/delete is handled by the separate secure-store adapter boundary and intentionally introduces no SQLite table, profile export schema change, cache persistence change, or plaintext key migration. Future migrations must preserve that provider keys remain outside SQLite, profile exports, diagnostics, and logs.
- 2026-05-28 T-007-006: `npm run smoke:config` and the configuration API smoke runbook add no runtime persistence. The smoke uses injected repository/secure-store fixtures and an ephemeral localhost port only; no SQLite migration, key storage migration, profile export migration, or cache migration is needed.
- 2026-05-28 T-008-001: Capture/OCR API contract validation is a pure validator slice. It introduces no SQLite tables, no capture image persistence, no OCR text persistence, no key storage movement, no cache persistence, and no profile export schema bump. Future capture/OCR route slices must keep captured images and raw OCR text out of default persistence and diagnostics.
- 2026-05-28 T-008-002: Capture source enumeration is an injected process-local API route. It introduces no SQLite tables, no persisted source-list cache, no captured image persistence, no OCR text persistence, no key storage movement, and no profile export schema bump. Future concrete Windows enumeration adapters must remain non-persistent by default.
- 2026-05-28 T-008-003: Manual OCR test is an injected process-local API route. It reads profile/ROI configuration but introduces no SQLite schema change, no OCR text persistence, no screenshot persistence, no captured image persistence, no key storage movement, no cache persistence, and no profile export schema bump. Future OCR adapters must keep test images and raw OCR text in memory unless a later explicit privacy opt-in slice adds durable storage.
- 2026-05-28 T-008-004: Capture start/stop runtime status is an injected process-local API route pair. It reads profile/capture source configuration and keeps active capture session/status in memory only; it introduces no SQLite schema change, no captured image persistence, no screenshot path persistence, no OCR text persistence, no key storage movement, no cache persistence, and no profile export schema bump. Future capture adapters must keep frames in memory by default unless a later explicit privacy opt-in slice adds durable storage.
- 2026-05-28 T-008-005: `npm run smoke:capture-ocr` and the capture/OCR smoke runbook add no runtime persistence. The smoke uses injected capture/OCR dependencies and an ephemeral localhost port only; no SQLite migration, key storage migration, profile export migration, OCR text persistence, screenshot persistence, captured image persistence, or cache migration is needed.
- 2026-05-28 T-009-001: Translation test API contract validation is a pure validator slice. It introduces no SQLite schema change, no translation cache rows, no provider key storage movement, no OCR text persistence, no translated text persistence, no logs/diagnostics persistence, and no profile export schema bump.
- 2026-05-28 T-009-002: `POST /api/translate/test` is an injected process-local API route. It reads profile/glossary/provider/target-language configuration from the existing schema version 1 tables but introduces no SQLite schema change, no translated text persistence, no supplied test text persistence, no translation cache write, no key storage movement, and no profile export schema bump. Future provider adapters must keep supplied text, provider responses, and translated text in memory unless a later explicit privacy opt-in slice adds durable storage.
- 2026-05-28 T-009-003: Translation runtime status broadcast is an in-memory override inside `createLocalApiServer`. It introduces no SQLite schema change, no translated text persistence, no supplied test text persistence, no translation cache write, no key storage movement, no logs/diagnostics persistence, and no profile export schema bump. The override is observable only through `GET /api/status` and `/ws/app` snapshots and resets when the process restarts; supplied text, translated text, glossary terms, cache keys, provider keys, and raw provider exceptions never reach durable storage through this slice.
- 2026-05-28 T-009-004: `npm run smoke:translation` and the translation API smoke runbook add no runtime persistence. The smoke uses injected translation dependencies and an ephemeral localhost port only; no SQLite migration, key storage migration, profile export migration, translation cache persistence, supplied text persistence, translated text persistence, logs/diagnostics persistence, or schema bump is needed.
- 2026-05-28 T-010-001: Diagnostics bundle contract validation and redaction is an in-memory contract slice. It introduces no SQLite schema change, no durable log store, no screenshot/image persistence or reads, no provider key storage movement, no profile export schema change, and no cache persistence. Future diagnostics route slices must keep bundle generation on-demand and redacted before return.
- 2026-05-28 T-010-002: `GET /api/diagnostics/bundle` is an injected process-local API route. It generates bundles on demand from optional in-memory diagnostic sources, applies redaction before response, and introduces no SQLite schema change, no durable log store, no screenshot/image persistence or reads, no plaintext key access, no provider key storage movement, no profile export schema change, and no cache persistence.
- 2026-05-28 T-010-003: `npm run smoke:diagnostics` and the diagnostics API smoke runbook add no runtime persistence. The smoke uses injected diagnostics sources and ephemeral localhost ports only; no SQLite migration, key storage migration, profile export migration, durable log store, screenshot/image persistence or reads, plaintext key access, raw OCR/source text persistence, translated text persistence, or cache migration is needed.
- 2026-05-28 T-011-001: Desktop UI shell route/state contract is a pure JS renderer module in `src/ui/desktop-shell.js` plus focused `test/desktop-shell.test.js` coverage. It introduces no SQLite schema change, no durable log store, no screenshot/image persistence or reads, no plaintext key access, no provider key storage movement, no profile export schema change, no cache persistence, and no npm dependency change. The view-model snapshot is in-memory only; the shell holds no durable state across process restarts and writes nothing to disk. Future renderer slices that wire concrete React components or the Electron host must keep this module as their consumed contract and must extend `RECOVERY_ACTIONS_BY_CODE` in the same slice that introduces a new backend `RuntimeStatus.code` or `ApiError.code`.
- 2026-05-28 T-011-002: First-Run provider/profile setup flow contract is a pure JS renderer module in `src/ui/first-run-flow.js` plus focused `test/first-run-flow.test.js` coverage. It introduces no SQLite schema change, no durable wizard state, no plaintext key access, no provider key storage movement, no profile export schema change, no cache persistence, no logs/diagnostics persistence, no screenshot/image persistence or reads, and no npm dependency change. The flow emits existing API request intents only; provider keys and manual translation test text are fetch-time-only values and are not stored in serializable state or log-safe descriptors.
- 2026-05-28 T-011-003: Home/Status action contract is a pure JS renderer module in `src/ui/home-status-actions.js` plus focused `test/home-status-actions.test.js` coverage. It introduces no SQLite schema change, no durable UI action history, no plaintext key access, no provider key storage movement, no profile export schema change, no cache persistence, no logs/diagnostics persistence, no screenshot/image persistence or reads, and no npm dependency change. Status refresh, WebSocket, clipboard, capture start/stop, route navigation, and host restart/troubleshooting behaviors are emitted as in-memory intent descriptors only; the module performs no I/O and writes nothing to disk.
- 2026-05-28 T-011-004: Capture/OCR/Translation setup-screen contract is a pure JS renderer module in `src/ui/setup-screens.js` plus focused `test/setup-screens.test.js` coverage. It introduces no SQLite schema change, no durable setup-screen state, no plaintext key access, no provider key storage movement, no profile export schema change, no cache persistence, no logs/diagnostics persistence, no screenshot/image persistence or reads, and no npm dependency change. Source enumeration, profile updates, OCR preview, provider-key save, and translation preview are emitted as existing API request intents only; API keys and manual translation test text are fetch-time-only values, while OCR/translated preview text stays transient and redacted from log-safe snapshots.
- 2026-05-28 T-011-005: Overlay/Privacy/Diagnostics closeout-screen contract is a pure JS renderer module in `src/ui/closeout-screens.js` plus focused `test/closeout-screens.test.js` coverage. It introduces no SQLite schema change, no durable theme preview state, no durable diagnostics state, no plaintext key access, no provider key storage movement, no profile export schema change, no cache persistence, no logs/diagnostics persistence, no screenshot/image persistence or reads, and no npm dependency change. Theme CRUD, profile theme assignment, OBS trusted URL actions, privacy settings, and diagnostics bundle copy are emitted as existing API/host intent descriptors only; diagnostics logs, debug paths, and bundle copy contents are redacted from log-safe snapshots.
- 2026-05-28 T-012-001: Synthetic first-run stream harness is a pure JS core contract in `src/core/synthetic-first-run-stream.js` plus focused `test/synthetic-first-run-stream.test.js` coverage. It creates only in-memory `OverlayState` and runtime-pipeline evidence, introduces no SQLite schema change, no profile export schema change, no provider key storage movement, no cache persistence, no durable logs, no OCR/source text persistence, no translated text persistence, no screenshot/image persistence or reads, no native dependency, and no npm dependency change.
- 2026-05-28 T-012-002: First-run stream smoke command is a dependency-free Node script in `scripts/smoke-first-run-stream.js` plus focused child-process coverage. It uses an ephemeral localhost API server, shared in-memory `OverlayState`, deterministic injected provider, and existing overlay HTTP/WebSocket contracts only. It introduces no SQLite schema change, no profile export schema change, no provider key storage movement, no cache persistence, no durable logs, no OCR/source text persistence, no translated text persistence, no screenshot/image persistence or reads, no native dependency, and no npm dependency change. The printed evidence is hash/id/count only.
- 2026-05-28 T-012-003: Backend recovery smoke command is a dependency-free Node script in `scripts/smoke-backend-recovery.js` plus focused child-process coverage. It uses in-memory `OverlayState`, ephemeral localhost API servers, and temporary localhost port blockers only. It introduces no SQLite schema change, no profile export schema change, no provider key storage movement, no cache persistence, no durable logs, no OCR/source text persistence, no translated text persistence, no screenshot/image persistence or reads, no native dependency, and no npm dependency change. The printed evidence is hash/code/port/check only.
- 2026-05-28 T-012-004: Overlay layout smoke command is a dependency-free Node script in `scripts/smoke-overlay-layout.js` plus focused child-process coverage. It renders the existing overlay HTML/CSS contract in memory for required OBS Browser Source resolutions, built-in themes, and line counts. It introduces no SQLite schema change, no profile export schema change, no provider key storage movement, no cache persistence, no durable logs, no OCR/source text persistence, no translated text persistence, no screenshot/image persistence or reads, no browser/OBS/native dependency, and no npm dependency change. The printed evidence is hash/count/resolution/metric only.
- 2026-05-28 T-012-005: First-run stream closeout smoke/runbook is a dependency-free Node script plus a Japanese runbook. It executes existing T-012 child smokes and records only hash/count/schema evidence. It introduces no SQLite schema change, no profile export schema change, no provider key storage movement, no cache persistence, no durable logs, no OCR/source text persistence, no translated text persistence, no screenshot/image persistence or reads, no browser/OBS/native dependency, no real provider credential use, and no npm dependency change.

## Migration Steps
1. Pre-checks: confirm current `app_meta.schema_version`, backup DB, verify disk write access, verify app is not actively capturing.
2. Data/schema migration: run forward-only migration in a transaction.
3. Verification: run integrity checks, count expected rows, verify API status, verify active profile loads.
4. Cutover: update `app_meta.schema_version` only after verification passes.

## Rollback Strategy
- Rollback trigger: migration transaction fails, active profile cannot load, or privacy invariant is violated.
- Rollback steps: abort transaction when possible; otherwise restore pre-migration backup.
- Data recovery notes: translation cache may be discarded; profiles, themes, glossary, and privacy settings must be preserved.

## Verification Checklist
- [ ] Functional checks pass.
- [ ] Regression tests pass.
- [ ] Active profile loads and can be exported without forbidden fields.
- [ ] API keys are absent from SQLite and JSON exports.
- [ ] Observability logs remain redacted.
- [ ] Error rate unchanged or improved.
